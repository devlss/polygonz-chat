import React from 'react';
import {render, screen} from '@testing-library/react';
import {ChatUsersListItemComponent} from '.';

describe('Stub tests for ChatUsersListItemComponent module', () => {
	test('Is hello world exists', () => {
		// render(<ChatUsersListItemComponent />);
		// const linkElement = screen.getByText(/HelloWorld!/i);
		// expect(linkElement).toBeInTheDocument();
	});
});
